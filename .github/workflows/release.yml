name: Lint, Version & Release

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  lint:
    name: Luacheck
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Luacheck
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq lua5.1 luarocks > /dev/null
          sudo luarocks install luacheck > /dev/null

      - name: Run Luacheck
        run: luacheck . --config .luacheckrc

      - name: No hard-coded client font
        # The Forever skin sets every font through BRutus:ApplyFont (Core/Data.lua),
        # which is what keeps serif at 14px and up and mono at 10px and up.
        # ChehulNet.lua is shared verbatim with other Chehul addons and cannot call
        # into BRutus, so it keeps the client font.
        run: |
          if grep -rn 'FRIZQT__' --include='*.lua' --exclude='ChehulNet.lua' Core Modules UI; then
            echo "Hard-coded client font found; use BRutus:ApplyFont." >&2
            exit 1
          fi

      - name: Version-sensitive calls go through Compat
        # Rule 4 (issue #10): items, spells, auras, bags, talents, skill lines, the roster
        # request, addon messages and what a tooltip is showing are called only from
        # Core/Compat.lua.
        run: lua5.1 tools/compat-guard.lua

      - name: The harnesses
        # Each tools/*.lua runs the real addon code under a stubbed client and exits 1 on the
        # first failed check. Until now nothing ran them: tools/roster-import.lua had been red
        # since ae991c2 and no one found out for a month.
        run: |
          sudo apt-get install -y -qq luajit > /dev/null
          python3 -m pip install --quiet pyyaml
          for f in tools/*.lua; do
            luajit -e 'ADDON="."' "$f" > /dev/null || { echo "$f failed" >&2; exit 1; }
            echo "ok $f"
          done
          # tools/beta-workflow.py is the only test of beta.yml and publish.yml, and it was red
          # through the whole of #20 because nothing ran it either.
          for f in tools/*.py; do
            python3 "$f" > /dev/null || { echo "$f failed" >&2; exit 1; }
            echo "ok $f"
          done

  release:
    name: Version & Release
    needs: lint
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Compute next version
        id: version
        run: |
          # Get latest semver tag, default to 0.0.0 if none
          LATEST=$(git tag --sort=-v:refname | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+$' | head -1)
          if [ -z "$LATEST" ]; then
            LATEST="v0.0.0"
          fi
          echo "Latest tag: $LATEST"

          # Strip 'v' prefix and split
          CURRENT="${LATEST#v}"
          IFS='.' read -r MAJOR MINOR PATCH <<< "$CURRENT"

          # Determine bump from commit messages since last tag
          COMMITS=$(git log "$LATEST"..HEAD --pretty=format:"%s" 2>/dev/null || git log --pretty=format:"%s")

          if echo "$COMMITS" | grep -qiE "^(feat|feature)(\(.+\))?!:|BREAKING CHANGE"; then
            MAJOR=$((MAJOR + 1)); MINOR=0; PATCH=0
          elif echo "$COMMITS" | grep -qiE "^(feat|feature)(\(.+\))?:"; then
            MINOR=$((MINOR + 1)); PATCH=0
          else
            PATCH=$((PATCH + 1))
          fi

          NEXT="${MAJOR}.${MINOR}.${PATCH}"
          echo "Next version: $NEXT"
          echo "next=$NEXT" >> "$GITHUB_OUTPUT"

      - name: Update TOC version
        run: |
          sed -i "s/^## Version: .*/## Version: ${{ steps.version.outputs.next }}/" GuildOS.toc

      - name: Update in-game version constant
        run: |
          sed -i -E "s/^(GuildOS\.VERSION[[:space:]]*=[[:space:]]*)\"[^\"]*\"/\1\"${{ steps.version.outputs.next }}\"/" Core/Config.lua

      - name: Generate changelog entry
        run: |
          VERSION="${{ steps.version.outputs.next }}"
          DATE=$(date +%Y-%m-%d)
          LATEST=$(git tag --sort=-v:refname | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+$' | head -1)

          # Collect commits since last tag, grouped by type
          if [ -n "$LATEST" ]; then
            COMMITS=$(git log "$LATEST"..HEAD --pretty=format:"%s" 2>/dev/null)
          else
            COMMITS=$(git log --pretty=format:"%s")
          fi

          ADDED=""
          FIXED=""
          CHANGED=""
          OTHER=""

          while IFS= read -r msg; do
            [ -z "$msg" ] && continue
            # Skip CI commits
            echo "$msg" | grep -qE "^\s*(chore: bump version|Merge)" && continue

            # Categorise by conventional commit prefix
            if echo "$msg" | grep -qiE "^feat(\(.+\))?[!]?:"; then
              line=$(echo "$msg" | sed -E 's/^[Ff]eat(\([^)]*\))?[!]?:\s*//')
              ADDED="${ADDED}\n- ${line}"
            elif echo "$msg" | grep -qiE "^fix(\(.+\))?:"; then
              line=$(echo "$msg" | sed -E 's/^[Ff]ix(\([^)]*\))?:\s*//')
              FIXED="${FIXED}\n- ${line}"
            elif echo "$msg" | grep -qiE "^(refactor|perf|style)(\(.+\))?:"; then
              line=$(echo "$msg" | sed -E 's/^[A-Za-z]+(\([^)]*\))?:\s*//')
              CHANGED="${CHANGED}\n- ${line}"
            else
              # Include non-chore/ci/docs commits as other changes
              echo "$msg" | grep -qiE "^(chore|ci|docs|test)(\(.+\))?:" && continue
              OTHER="${OTHER}\n- ${msg}"
            fi
          done <<< "$COMMITS"

          # Build the entry
          ENTRY="## [${VERSION}] - ${DATE}"
          [ -n "$ADDED" ]   && ENTRY="${ENTRY}\n\n### Added${ADDED}"
          [ -n "$FIXED" ]   && ENTRY="${ENTRY}\n\n### Fixed${FIXED}"
          [ -n "$CHANGED" ] && ENTRY="${ENTRY}\n\n### Changed${CHANGED}"
          [ -n "$OTHER" ]   && ENTRY="${ENTRY}\n\n### Other${OTHER}"

          # Prepend to CHANGELOG.md (after the header line)
          if [ -f CHANGELOG.md ]; then
            # Insert after the first two lines (title + blank line)
            head -n 3 CHANGELOG.md > CHANGELOG.tmp
            echo "" >> CHANGELOG.tmp
            echo -e "$ENTRY" >> CHANGELOG.tmp
            echo "" >> CHANGELOG.tmp
            tail -n +4 CHANGELOG.md >> CHANGELOG.tmp
            mv CHANGELOG.tmp CHANGELOG.md
          else
            echo -e "# Guild OS Changelog\n\nAll notable changes to this project will be documented in this file.\n\n${ENTRY}" > CHANGELOG.md
          fi

      - name: Commit version bump
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add GuildOS.toc Core/Config.lua CHANGELOG.md
          git diff --cached --quiet && echo "No changes" && exit 0
          git commit -m "chore: bump version to ${{ steps.version.outputs.next }} [skip ci]"
          git push origin main

      - name: Create and push tag
        run: |
          git tag -a "v${{ steps.version.outputs.next }}" -m "Release v${{ steps.version.outputs.next }}"
          git push origin "v${{ steps.version.outputs.next }}"

      - name: Trigger publish workflow
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'publish.yml',
              ref: 'v${{ steps.version.outputs.next }}',
            });
